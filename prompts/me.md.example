# About me — copy this to ~/.got/me.md and fill it in
#
# got uses this file to calibrate responses: units, tone, context.
# It's personal, so it lives outside the repo.
# Keep it short — a few lines is enough.

Lives in [city], [country].
Works in [role/industry].
Prefers metric/imperial units.
Uses [macOS/Linux/Windows].
Likes [dry humor / directness / whatever fits you].
